# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

# Default values for remote-access-proxy.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
---

global:
  registry:
    name: null
    imagePullSecrets:
      - name: ""

replicaCount: 1

image:
  repository: infra/rmtaccessproxy
  pullPolicy: IfNotPresent
  # Image specific takes precedence
  # registry:
  #   name: null
  #   imagePullSecrets:
  #     - name: ""

nameOverride: ""
fullnameOverride: "remote-access-proxy"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  seccompProfile:
    type: RuntimeDefault

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  runAsUser: 65534

service:
  type: ClusterIP
  rmtAccessproxy:
    name: rmtaccessproxy-api
    port: 8080

metrics:
  enabled: false
  port: 8081

inventory:
  addr: inventory
  port: 50051

rmtAccessproxy:
  enableTracing: false
  traceURL: "fluent-bit:4318"
  oamservaddr: "0.0.0.0:2379"

# Sets requests/limits to support at least 100 edge nodes.
# Must be adjusted in case of higher load.
resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "100m"
    memory: "128Mi"

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

globalLogLevel: "info"

# -- traefikReverseProxy defines params for external access to services in a cluster using Traefik reverse proxy.
# Expose RAP via Traefik (HTTP/WebSocket)
traefikReverseProxy:
  host:
    http:
      name: "remote-access-proxy.domain"

  enabled: true
  # -- gatewayNamespace specifies namespace of Traefik and related resources (e.g. k8s secret or tls option).
  gatewayNamespace: orch-gateway
  # -- secretName is name of k8s secret containing TLS server cert.
  secretName: tls-demo
  # -- tlsOption is the name of the Traefik option for handling TLS or mTLS connections.
  tlsOption: gateway-tls
  # Optional: override route match (otherwise chart should build it from host.http.name)
  matchRoute: ""

traefikApiGroup: "traefik.containo.us/v1alpha1"

oidc:
  oidc_server_url: "http://platform-keycloak.orch-platform/realms/master"
  oidc_tls_insecure_skip_verify_value: "true"
  clients:
    # REST clients should set the "User-Agent" header to be the client name.
    bypass:

serviceArgs:
  enableAuth: true
  invCacheUuidEnable: true
  invCacheStaleTimeout: 10m
